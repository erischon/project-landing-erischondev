---
type Props = {
  id: string;
  background: string;
  specificStyle: string;
};

const { id, background, specificStyle } = Astro.props;
---

<section
  id={id}
  class={`${background} ${specificStyle} max-w-6xl mx-auto px-2 py-[5%] lg:px-0 lg:py-[7%]`}
>
  <slot />
</section>
